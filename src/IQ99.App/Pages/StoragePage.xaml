<Page x:Class="IQ99.App.Pages.StoragePage"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
          mc:Ignorable="d"
          d:DesignWidth="760" d:DesignHeight="560">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <ComboBox x:Name="DriveCombo" Width="300" />
            <ui:Button x:Name="BtnScan" Content="Analizar disco" Click="BtnScan_Click" Margin="10,0,0,0" Appearance="Primary" />
            <ui:Button x:Name="BtnEmptyBin" Content="Vaciar papelera" Click="BtnEmptyBin_Click" Margin="10,0,0,0" />
        </StackPanel>

        <ListView x:Name="FolderListView" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Carpeta" Width="420">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path}" TextTrimming="CharacterEllipsis" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Tamaño" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SizeText}" TextAlignment="Right" Foreground="{ui:ThemeResource TextFillColorAccentBrush}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Uso relativo" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding Percent}" Maximum="100" Height="14" Width="150" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <ProgressBar x:Name="FolderProgress" Grid.Row="2" Height="4" Margin="0,12,0,0" />
    </Grid>
</Page>