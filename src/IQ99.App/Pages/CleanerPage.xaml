<Page x:Class="IQ99.App.Pages.CleanerPage"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
          x:Name="CleanerPageRoot"
          mc:Ignorable="d"
          d:DesignWidth="760" d:DesignHeight="560">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView x:Name="CleanListView" Grid.Row="0" SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Limpiar" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Categoría" Width="220" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Descripción" Width="320" DisplayMemberBinding="{Binding Description}" />
                    <GridViewColumn Header="Tamaño" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SizeText}" TextAlignment="Right" Foreground="{ui:ThemeResource TextFillColorAccentBrush}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button x:Name="BtnAnalyze" Content="Analizar" Click="BtnAnalyze_Click" Appearance="Primary" />
            <ui:Button x:Name="BtnClean" Content="Limpiar seleccionados" Click="BtnClean_Click" Appearance="Danger" Margin="10,0,0,0" />
            <TextBlock x:Name="TxtCleanStatus" Grid.Column="2" VerticalAlignment="Center" Margin="14,0,0,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
            <StackPanel Grid.Column="3" Orientation="Horizontal">
                <ui:Button Content="Todo" Click="BtnSelectAll_Click" />
                <ui:Button Content="Ninguno" Click="BtnSelectNone_Click" Margin="8,0,0,0" />
            </StackPanel>
        </Grid>

        <ProgressBar x:Name="CleanProgress" Grid.Row="2" Height="4" Margin="0,12,0,0" />
    </Grid>
</Page>